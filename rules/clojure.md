---
paths: "**/*.{clj,cljs,cljc,edn}"
---

When working with Clojure files, invoke the appropriate skills:

- **Code changes**: invoke `clojure-edit` for structural editing best practices and form-aware scripts
- **Test files** (`**/test/**/*`): also invoke `clojure-testing` for testing patterns and practices
- **REPL-driven development**: invoke `clojure-repl` when iteratively developing or debugging
- **Malli schemas**: invoke `clojure-malli-schema` when working with Malli schemas
- **Project configuration**: invoke `clojure-project-setup` when working with deps.edn, bb.edn, or project structure
